# from frappe.utils import format_duration
# import frappe

# @frappe.whitelist()
# def get_format_duration(minutes):
#     minutes = int(minutes)
    
#     if minutes > 100000:
#         return "Duration too large to format"
    
#     return format_duration(minutes)